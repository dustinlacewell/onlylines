import { registerPalette } from '../core/palette';

export default registerPalette({
  id: 14,
  name: 'monochrome',
  category: 'neutral',
  description: 'Pure white',
  colors: ['hsl(0, 0%, 100%)'],
});
